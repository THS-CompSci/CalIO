<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="io.damen.calendar.event_create">

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Title"
        android:id="@+id/add_event_title" />

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Description"
        android:layout_below="@+id/add_event_title"
        android:layout_alignParentStart="true"
        android:id="@+id/add_event_description"/>
    <EditText
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:id="@+id/add_event_label"
        android:layout_below="@+id/add_event_description"
        android:layout_alignEnd="@+id/add_event_description"
        android:hint="Label"/>
    <Spinner
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/add_event_spinner"
        android:layout_alignTop="@+id/add_event_label"
        android:layout_alignParentStart="true"
        android:layout_toStartOf="@+id/add_event_label">
    </Spinner>
    <Button
        android:layout_width="185dp"
        android:layout_height="wrap_content"
        android:id="@+id/add_event_date"
        android:hint="Date"
        android:layout_below="@+id/add_event_spinner"
        android:layout_alignParentStart="true"
        android:layout_alignEnd="@+id/add_event_spinner" />
    <Button
        android:layout_width="175dp"
        android:layout_height="wrap_content"
        android:hint="Time"
        android:id="@+id/add_event_time"
        android:layout_below="@+id/add_event_spinner"
        android:layout_alignEnd="@+id/add_event_description" />
    <Button
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:hint="Create"
        android:id="@+id/add_event_create"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true" />

</RelativeLayout>
